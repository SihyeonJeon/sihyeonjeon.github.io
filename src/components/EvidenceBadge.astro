---
interface Props {
	label: string;
	muted?: boolean;
}

const { label, muted = false } = Astro.props;
---

<span class:list={['evidence-badge', muted && 'evidence-badge--muted']}>
	{muted ? '○' : '●'} {label}
</span>
