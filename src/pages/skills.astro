---
import SectionHead from '../components/SectionHead.astro';
import { skillDomains } from '../data/portfolio';
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Skills" description="Skill map for AI/Data systems work">
	<section class="page-head">
		<div class="shell">
			<div class="label">04 · Skills</div>
			<h1>Skills tied to artifacts, not ratings</h1>
			<p>
				Each item points to a repo, system, or work queue
			</p>
		</div>
	</section>

	<section class="section">
		<div class="shell">
			<SectionHead label="Skill map" title="What each capability points to" />
			{
				skillDomains.map((domain, index) => (
					<div class="skill-domain">
						<div class="skill-domain__head">
							<span>{String(index + 1).padStart(2, '0')}</span>
							<div>
								<h3>{domain.name}</h3>
								<p>{domain.sub}</p>
							</div>
						</div>
						<div class="skill-grid">
							{domain.items.map((item) => (
								<div class="skill-item">
									<strong>{item.name}</strong>
									<span>{item.ev}</span>
									<small>/{item.ref} · {item.signal}</small>
								</div>
							))}
						</div>
					</div>
				))
			}
		</div>
	</section>
</BaseLayout>
